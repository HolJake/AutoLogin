package com.autologin.mixin;

// Unused — kept as placeholder. The formatter field name changed in MC 1.21.11;
// password masking is handled via a render overlay in ChatScreenMixin instead.
public interface EditBoxFormatterAccessor {}
